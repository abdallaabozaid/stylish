// enum ConnectionType { wifi, mobile }
enum FormType { signIn, signUp }
enum AppTheme {
  lightTheme,
  darkTheme,
}

enum CategoryName { shirt, tshirt, pants, dress, allCategories }

enum SectionName { newArrivals, topTrending, featured, allSections }
